name: Sensor CI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  run-sensor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: python3 sensor.py || true
